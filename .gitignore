# Reserved for future Eidos tooling cache; not your specs
.eidos/

# Personal Obsidian vault config, if you open the repo as a vault
.obsidian/

# Vendored skill assets — generated by scripts/sync-skills.sh. The canonical copies
# live at the repo root and ship with the plugin, so these duplicates aren't committed.
# Run the script after cloning (or before packaging a skill for standalone use).
skills/eidos/templates/
skills/eidos-init/templates/
skills/eidos-init/EIDOS.md
skills/eidos-migrate/versions/
skills/eidos-migrate/CHANGELOG.md
skills/eidos-migrate/EIDOS.md

# Build output (scripts/package-plugin.sh)
dist/

# OS / editor noise
.DS_Store
